---
interface Props {
  name: string;
  href?: string;
}

const { name, href } = Astro.props;
const Tag = href ? "a" : "div";
---

<Tag href={href} class="badge badge-md badge-neutral text-neutral-content">
  {name}
</Tag>

<style>
  .badge {
    width: fit-content;
  }
</style>
